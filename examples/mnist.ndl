import n;
import s;

network
    neurons
        layer : in 784 x 1 = n;
        layer : h 125 x 1 = n;
        layer : out 10 x 1 = n;
    end neurons

    connections
        (0, [ALL]) : connect (1, [ALL]) = s;
        (1, [ALL]) : connect (2, [ALL]) = s;
    end connections
end network
