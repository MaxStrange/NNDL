import n;
import s;

network
    neurons
        layer : in 2 x 1 = n;
        layer : out 1 x 1 = n;
    end neurons

    connections
        (0, [ALL]) : connect (1, [ALL]) = s;
    end connections
end network
